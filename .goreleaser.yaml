version: 2
release:
  disable: "{{ .Env.GORELEASER_DISABLE_SCM_RELEASE }}"
before:
  hooks:
    - ./scripts/check-sitectl-core-version.sh v1.8.0
    - go mod tidy
builds:
  - binary: sitectl-omeka-s
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }} -X main.commit={{ .Commit }} -X main.date={{ .Date }}
    goos:
      - linux
      - windows
      - darwin

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: "checksums.txt"
snapshot:
  version_template: "{{ incpatch .Version }}-next"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^.github"
brews:
  - name: sitectl-omeka-s
    description: "A sitectl plugin for Omeka S stacks"
    homepage: "https://github.com/libops/sitectl-omeka-s"
    url_template: "https://github.com/libops/sitectl-omeka-s/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    license: MIT
    commit_author:
      name: sitectl-dev[bot]
      email: 2408410+sitectl-dev[bot]@users.noreply.github.com
    repository:
      owner: libops
      name: homebrew
      token: "{{ .Env.HOMEBREW_REPO_TOKEN }}"
    dependencies:
      - name: "libops/homebrew/sitectl"
nfpms:
  - id: sitectl-omeka-s
    package_name: sitectl-omeka-s
    file_name_template: "{{ .ConventionalFileName }}"
    vendor: libops
    homepage: "https://github.com/libops/sitectl-omeka-s"
    description: "A sitectl plugin for Omeka S stacks"
    maintainer: "Joe Corall <joe@libops.io>"
    license: "MIT"
    formats:
      - apk
      - deb
      - rpm
    mtime: "{{ .CommitDate }}"
    ids:
      - sitectl-omeka-s
    dependencies:
      - sitectl
    overrides:
      deb:
        dependencies:
          - sitectl (>= 1.8.2)
      rpm:
        dependencies:
          - sitectl >= 1.8.2
      apk:
        dependencies:
          - sitectl>=1.8.2
